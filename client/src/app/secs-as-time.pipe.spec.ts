import { SecsAsTimePipe } from './secs-as-time.pipe';

describe('SecsAsTimePipe', () => {
  it('create an instance', () => {
    const pipe = new SecsAsTimePipe();
    expect(pipe).toBeTruthy();
  });
});
